import React from 'react';

const uiReducer = (state={}, action) => {
    Object.freeze(state);
    switch(action.type) {
        // case :
        
        default:
            return state
    }
}

export default uiReducer